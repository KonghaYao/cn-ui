import { JSX, JSXElement } from 'solid-js';
/**
 * @title Mask
 */
export interface MaskProps {
    /**
     * @zh 点击回调
     * @en Callback when avatar is clicked
     */
    onClick?: (e) => void;
}
